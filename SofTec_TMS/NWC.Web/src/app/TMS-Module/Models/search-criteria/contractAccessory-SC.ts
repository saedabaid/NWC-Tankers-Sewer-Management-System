import { PageFilter } from "../common/page-fillter-model";

export class contractAccessorySC {
    PageFilter: PageFilter = new PageFilter();
    AccessoryIDs: number[];
    StationIDs: string[];
    ContractID: number;
}